# Steps to build this project
- Check out this git repository
- You don't have to explicitly handle any dependencies because it is using Swift Package Manager which will perform necessary tasks
- Open in Xcode
- Build
